# Checklist

- [x] L'analisi ha inventariato tutti i file testuali in `LosAngeles2024/legacy`
- [x] L'analisi ha classificato world, family, character, relationship, scenario, visual, combat, psyche, e state data
- [x] L'analisi ha identificato conflitti, duplicazioni, riferimenti mancanti, e dati canon-sensitive
- [x] Gli script generati rispettano ES5 e usano solo `var`
- [x] Gli script runtime non usano feature ristrette: `import`, `export`, `require`, `async`, `await`, `Promise`, `fetch`, `XMLHttpRequest`, `document`, `window`, `localStorage`, `sessionStorage`, `IndexedDB`, `fs`, `readFile`, `eval`, `new Function`
- [x] Gli script runtime usano `context` come unica interfaccia JanitorAI
- [x] Gli script modificano solo `context.character.personality`, `context.character.scenario`, e `context.character.example_dialogs`
- [x] Gli script applicano append-only injection e prefix corretti per personality/scenario/example dialogs
- [x] Gli script includono guardie per `context`, `context.character`, `context.character.personality`, e `context.character.scenario`
- [x] Gli script includono validazione e gestione sicura di stato mancante o invalido
- [x] Gli script includono meccanismi di persistenza coerenti e marcatori unici quando necessario
- [x] Gli script non introducono riferimenti a `database_old/`
- [x] Ogni lorebook voice include attribuzione `database/` e Canon Layer tag
- [x] Le categorie NPC seguono il modello aggiornato: `identity`, `appearance`, `relationships`, `personality`, `psyche`, `advancedPsychology`, `backstory`, `dialogue`, `combat`, `capabilities`, `sampleDialog`, `residence`, `intimacy`, `notes`
- [x] I dati NPC principali sono stati riconciliati con legacy canon per età, visual phenotype e relazioni familiari
- [x] I test unitari coprono parsing, mapping, validazione, injection append-only, stato invalido, e compatibilità runtime
- [x] I test unitari vengono eseguiti con esito positivo
- [x] La documentazione inline è presente senza duplicare documentazione esterna non richiesta
- [x] I file generati sono coerenti con `.trae/rules/rules.md`, `README.md`, e i template ufficiali
